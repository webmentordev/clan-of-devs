## Tech Stack

Laravel 13, Livewire 4, Reverb, AlpineJS, TailwindCSS


## Migration
$ php artisan migrate


## Admin abilities
```
* Create new channels
* Add user to a channel unless already a member
* Can not see the private channel if not a member
* Only Owner can add users
```